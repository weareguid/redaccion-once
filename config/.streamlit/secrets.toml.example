# Configuración de Once Noticias - Sistema Editorial Optimizado
# Copia este archivo a secrets.toml y completa con tus datos reales

# ✅ REQUERIDO: API Key de OpenAI (sistema no funcionará sin esta)
OPENAI_API_KEY = "sk-your-openai-api-key-here"

# ⚠️ OPCIONAL: Configuración de Snowflake
# Si no configuras esto, el sistema usará almacenamiento local
# El sistema funcionará perfectamente sin Snowflake

# SNOWFLAKE_ACCOUNT = "your-account.region"
# SNOWFLAKE_USER = "your-username"
# SNOWFLAKE_PASSWORD = "your-password"
# SNOWFLAKE_WAREHOUSE = "your-warehouse"
# SNOWFLAKE_DATABASE = "your-database"
# SNOWFLAKE_SCHEMA = "your-schema"

# 📝 NOTAS IMPORTANTES:
# 1. OPENAI_API_KEY es OBLIGATORIA - sin ella el sistema no funcionará
# 2. Las configuraciones de Snowflake están comentadas (líneas que empiezan con #)
# 3. Si quieres usar Snowflake, descomentaría las líneas quitando el # del inicio
# 4. Si no configuras Snowflake, el sistema guardará todo localmente en data/metrics/
# 5. Puedes probar el sistema inmediatamente solo con OPENAI_API_KEY

# 🔧 CONFIGURACIÓN AVANZADA (opcional)
# Puedes cambiar estas configuraciones en config/settings.py:
# - ENABLE_DATABASE_STORAGE = True/False (para forzar uso de Snowflake)
# - LOCAL_STORAGE_ENABLED = True/False (para habilitar/deshabilitar almacenamiento local)
# - LOCAL_STORAGE_PATH = "ruta/personalizada" (para cambiar dónde se guardan los archivos locales)

# ===== CONFIGURACIÓN APIS EXTERNAS (OPCIONAL - Para versiones futuras) =====
# APIs para inyección de datos en tiempo real
INEGI_API_KEY = "tu_inegi_api_key_opcional"
BANXICO_API_KEY = "tu_banxico_api_key_opcional"
NEWS_API_KEY = "tu_news_api_key_opcional"

# ===== CONFIGURACIÓN DE DESARROLLO (OPCIONAL) =====
# Para debugging y desarrollo
DEBUG_MODE = false
LOG_LEVEL = "INFO"

# ===== NOTAS DE CONFIGURACIÓN =====
# 1. NUNCA subas este archivo con secrets reales al repositorio
# 2. La API de OpenAI es REQUERIDA para que funcione el sistema
# 3. Snowflake es requerido para guardar métricas y contenido
# 4. Las APIs externas son opcionales y para funcionalidad futura